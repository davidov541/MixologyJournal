<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MixologyJournalApp.View"
             mc:Ignorable="d"
             x:Class="MixologyJournalApp.View.RecipePage"
             Title="{Binding Path=Name}">
    <ContentPage.Resources>
        <DataTemplate x:Key="evenTemplate">
            <ViewCell>
                <Label BackgroundColor="LightGray" Text="{Binding}" VerticalTextAlignment="Center" Padding="5,0" />
            </ViewCell>
        </DataTemplate>
        <DataTemplate x:Key="unevenTemplate">
            <ViewCell>
                <Label BackgroundColor="WhiteSmoke" Text="{Binding}" VerticalTextAlignment="Center" Padding="5,0" />
            </ViewCell>
        </DataTemplate>
        <local:AlternateColorDataTemplateSelector x:Key="alternateColorDataTemplateSelector"
                                                  EvenTemplate="{StaticResource evenTemplate}"
                                                  UnevenTemplate="{StaticResource unevenTemplate}" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <ScrollView Orientation="Vertical" 
                        Margin="5" 
                        VerticalOptions="StartAndExpand" 
                        AbsoluteLayout.LayoutBounds="0,0,1,1" 
                        AbsoluteLayout.LayoutFlags="All">
                <StackLayout Orientation="Vertical">
                    <ListView x:Name="ingredientUsageList"
                              ItemTemplate="{StaticResource alternateColorDataTemplateSelector}"
                              ItemsSource="{Binding IngredientUsages}"
                              RowHeight="40"
                              Margin="0,0,0,10" 
                              VerticalScrollBarVisibility="Never" />
                    <Label Text="{Binding Path=FormattedSteps}" />
                </StackLayout>
            </ScrollView>
            <Button x:Name="deleteRecipe" 
                    Image="@drawable/ic_trash_48dp"
                    Command="{Binding DeleteRecipeCommand}"
                    IsVisible="{Binding Path=IsAdminUser}"
                    AbsoluteLayout.LayoutFlags="PositionProportional"
                    AbsoluteLayout.LayoutBounds="1, 1, AutoSize, AutoSize"
                    Style="{StaticResource FloatingButton}" />
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>