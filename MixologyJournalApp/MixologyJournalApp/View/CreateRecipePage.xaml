<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MixologyJournalApp.View.CreateRecipePage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Steps" />
            <ListView x:Name="StepsList" 
                      ItemsSource="{Binding Path=StepsList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Entry Text="{Binding Path=Text, Mode=TwoWay}" />
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:Name="addStepButton" 
                Text="Add Step" 
                MinimumHeightRequest="30" 
                Clicked="addStepButton_Clicked" />
            <Button x:Name="createButton" 
                Text="Create" 
                MinimumHeightRequest="30" 
                Clicked="createButton_Clicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>