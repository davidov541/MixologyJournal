<UserControl
    x:Class="MixologyJournal.View.ImageList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MixologyJournal.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="uc">

    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Name="VisibilityConverter" />
   </UserControl.Resources>

    <Grid>
        <ListView x:Name="ImagesList" ItemsSource="{Binding PicturePaths, ElementName=uc}" Margin="0,5" 
              SelectionChanged="Pictures_SelectionChanged" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding}" Width="80" Height="80" Margin="0,5" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Visibility="{Binding Busy, ElementName=uc, Converter={StaticResource VisibilityConverter}}">
            <Grid.Background>
                <SolidColorBrush Color="Black" Opacity="0.50" />
            </Grid.Background>
            <ProgressBar IsIndeterminate="True" VerticalAlignment="Center" HorizontalAlignment="Stretch" />
        </Grid>
    </Grid>
</UserControl>
