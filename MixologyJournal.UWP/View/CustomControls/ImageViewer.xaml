<UserControl
    x:Class="MixologyJournal.View.ImageViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MixologyJournal.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="uc">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image x:Name="FullImage" Source="{Binding Picture, ElementName=uc}" VerticalAlignment="Center" 
               LayoutUpdated="FullImage_LayoutUpdated">
            <Image.RenderTransform>
                <CompositeTransform Rotation="90" />
            </Image.RenderTransform>
        </Image>
        <Button Click="ClosePictureButton_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15">
            <SymbolIcon Symbol="Cancel" />
        </Button>
    </Grid>
</UserControl>
